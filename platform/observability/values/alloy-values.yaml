# Disabled because monitoring.coreos.com ServiceMonitor CRDs are not installed in the VictoriaMetrics-only baseline.
alloy:
  configMap:
    create: false
    name: alloy-logs-config

controller:
  type: daemonset

serviceMonitor:
  enabled: false

resources:
  requests:
    cpu: 100m
    memory: 128Mi
  limits:
    cpu: 500m
    memory: 512Mi

rbac:
  create: true

serviceAccount:
  create: true
